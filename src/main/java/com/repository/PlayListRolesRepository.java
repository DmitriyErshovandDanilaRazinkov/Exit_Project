package com.repository;

import com.model.PlayListRoles;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlayListRolesRepository extends JpaRepository<PlayListRoles, Long> {
}
